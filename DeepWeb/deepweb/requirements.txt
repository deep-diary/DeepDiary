# DeepWeb 依赖包 - Python 3.13兼容版本
# 核心框架
streamlit>=1.39.0
plotly>=5.17.0
pandas>=2.0.0

# MQTT通信
paho-mqtt>=1.6.1

# 图像处理
# 注意：OpenCV 需要使用 NumPy 1.x 版本，NumPy 2.x 不兼容
# 如果使用 opencv-python-headless，确保 NumPy 版本 < 2.0
opencv-python-headless>=4.8.0  # 推荐使用 headless 版本，避免 GUI 相关依赖
numpy>=1.24.0,<2.0  # 必须使用 NumPy 1.x，OpenCV 不支持 NumPy 2.x
Pillow>=10.0.0

# 数据处理
dataclasses-json>=0.6.0

# 配置管理
pyyaml>=6.0
gradio>=5.49.1
keyboard>=0.13.5

# 注意：以下模块是Python标准库，无需安装
# sqlite3 - Python标准库
# pathlib - Python标准库  
# threading - Python标准库
# json - Python标准库
# time - Python标准库
# datetime - Python标准库
