---
alwaysApply: true
---

# DeepWin 开发规则与项目指南

## 1. 配置管理器和日志管理器

整个项目需要统一使用配置管理器和日志管理器，其路径分别如下：

- **日志管理器**: `deepwin/datamanagement/log_manager.py`
- **配置管理器**: `deepwin/datamanagement/config_manager.py`

## 2. 功能包规则

### 2.1 文档要求

- 每个功能包都必须包含一个 `README.md` 文件，包括：
  - 当前包的目录结构
  - 相关功能逻辑
  - 主要的类介绍
  - 使用说明等
- 每次在包内更新相关需求时，只有在收到我更新 README 的指令后，再检查 `README.md` 是否需要更新，因为有时候过程更新并未验证的时候，并不需要更新

### 2.2 初始化文件

- 每个功能包都必须包含一个初始化文件 `__init__.py`
- 每次新增类时，都要考虑是否需要更新这个文件

### 2.3 接口设计原则

- 每个核心功能模块或包，对外应只暴露一个主要的公共接口类
- 所有外部模块与该功能模块的交互都应通过这个接口类进行，以实现高内聚低耦合

### 2.4 Demo 文件要求

- 每个重要的功能模块（或包，尤其是提供公共接口的包）都必须包含一个 `demo.py` 文件
- `demo.py` 必须是一个可独立运行的脚本，用于演示该模块的主要功能和接口用法
- 包含清晰的示例代码和注释，展示如何实例化模块的公共接口类，并调用其主要方法
- 如果功能模块代码量比较大，可拆分车多个 demo 文件进行编写

### 2.5 接口文档规范

每个公共接口类的方法和重要功能函数，其 docstrings 必须详细说明：

- 输入参数的类型、含义
- 输出结果的格式、类型和含义
- 作为一份"接口文档"，便于其他开发者（特别是 UI 开发人员）调用

## 3. 编码规范与最佳实践

### 3.1 PEP 8 规范

- 严格遵循 Python PEP 8 编码规范
- 包括命名约定、代码格式、空格使用等

### 3.2 类型提示 (Type Hints)

- 所有函数签名、类属性和复杂变量都应使用类型提示
- 提高代码可读性和可维护性

### 3.3 错误处理

- 使用 `try-except` 块进行健壮的错误处理，避免程序崩溃
- 自定义异常类型（如 `deepwin/utils/exceptions.py`）来表示特定业务或系统错误
- 对可能失败的外部调用（如网络请求、串口通信）进行合理的重试机制

### 3.4 日志记录

- 使用 Python 标准 `logging` 模块进行日志记录
- 区分日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
- 避免使用 `print()` 进行调试输出
- 日志应通过 `deepwin/data_management/log_manager.py` 统一管理

### 3.5 异步编程

- 对于 I/O 密集型操作（如网络通信、文件读写、设备等待），优先考虑使用 `asyncio` 和 `await/async` 关键字
- 提高应用程序的响应性和并发能力

### 3.6 资源管理

- 确保文件句柄、网络连接、串口连接等资源在使用完毕后正确关闭，避免资源泄露
- 使用 `with` 语句处理文件和部分连接

## 4. 文档与注释

### 4.1 Docstrings

- 所有类、方法和函数都必须编写清晰、简洁的 docstrings
- 解释其目的、参数、返回值、可能抛出的异常
- 推荐使用 Google 或 Sphinx 风格

### 4.2 行内注释

- 对于复杂或不易理解的逻辑，添加必要的行内注释进行解释

## 5. 可测试性

### 5.1 单元测试

- 编写可测试的代码，确保每个函数和方法的功能独立且可验证

### 5.2 测试用例

- 在 `tests/` 目录下为主要功能模块编写单元测试
- 覆盖核心逻辑和边界情况

## 6. 可扩展性与 Agent-Ready 设计

### 6.1 接口抽象

- 在设计模块接口时，考虑未来可能的功能扩展
- 尽量使用抽象基类或协议定义接口，而不是具体实现

### 6.2 Agent 友好

- 在设计任何模块时，请记住 DeepWin 最终将集成智能体（Agent）层
- 模块对外暴露的接口应便于 Agent 层进行感知（获取数据）和行动（触发操作）
- 例如，提供清晰的事件发布/订阅机制

### 6.3 松耦合

- 避免模块之间直接依赖具体实现，而是依赖抽象接口或消息传递

## 7. 版本控制

### 7.1 原子性提交

- 每次提交应专注于一个逻辑变更
- 编写清晰、有意义的提交信息

## 8. 终端说明

- 目前终端版本不支持 && 指令
- 如果要运行代码，第一次打开终端，需要激活项目环境, 命令：conda activate deepwin

---

**重要提醒**: 请严格遵守这些规则，并在开发过程中不断自我检查。这将使您成为一个高效、专业的开发伙伴。

请严格遵守这些规则，并在开发过程中不断自我检查。这将使您成为一个高效、专业的开发伙伴。
